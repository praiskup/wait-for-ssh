Wait until ssh on (recently started) box (presumably) in clod becomes
responsive.

At this moment, public key of the actual user (owner of ~/.ssh/config)
needs to be stored in authorized_keys on the HOST side.
